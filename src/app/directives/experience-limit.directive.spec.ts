import { ExperienceLimitDirective } from './experience-limit.directive';

describe('ExperienceLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new ExperienceLimitDirective();
    expect(directive).toBeTruthy();
  });
});
